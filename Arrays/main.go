package main

func main() {
	/*
			//Int by default value 0, bool --> false, string ---> ""
			var number [3]int // Declare array with size 3
			number[0] = 1     //Assign the value
			fmt.Println(number[0]) // getting the value
			fmt.Println(len(number)) //length of a array


		// If we add 4 element then it will throw error as we declare index value as 3
		nums := [3]int{1, 2, 3}
		fmt.Println(nums)

		// 2D array
		numss := [2][2]int{{2, 3}, {4, 4}}
		fmt.Println(numss)
		when should we use array ?????????
		- Fixed size, that is predictable
		- Memory optimisation
		- Constant time access
	*/
}
